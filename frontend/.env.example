# Optional: only needed to point at a backend on a different host/port than the one this page
# was loaded from. Leave unset for normal local dev or LAN/Tailscale access.
# VITE_API_BASE=http://localhost:8000
